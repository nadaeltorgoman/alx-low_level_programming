#include <stdio.h>

/**
 * main - entry point
 *
 * Description: A C program that prints the alphabet in lowercase.
 *
 * Return: Always 0 (Success)
 **/

int main(void)
{
	for (char c = a; c <= z; c++;)
		putchar(i);
	putchar('\n');
	return (0);
}
