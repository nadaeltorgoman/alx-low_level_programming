#include "main.h"
/**
 * add - a function that adds two integers and returns the result
 * @one: int to be added to two
 * @two: int to be added to one
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
